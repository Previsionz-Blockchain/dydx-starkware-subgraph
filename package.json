{
  "name": "dydx-balances",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ Previsionz-Block-Data/dydx-balances",
    "create-local": "graph create --node http://localhost:8020/ previsionz-block-data/dydx-balances",
    "remove-local": "graph remove --node http://localhost:8020/ previsionz-block-data/dydx-balances",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 previsionz-block-data/dydx-balances",
    "test": "graph build && graph test CallProxy"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.22.3",
    "@graphprotocol/graph-ts": "^0.22.1",
    "matchstick-as": "^0.1.5"
  }
}
